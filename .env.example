# Copy to .env and fill in. For Railway, set these in the dashboard (Variables).
# SQLite (local or Railway volume):
DATABASE_URL="file:./dev.db"

# On Railway with a volume mounted at /data use:
# DATABASE_URL="file:/data/production.db"
